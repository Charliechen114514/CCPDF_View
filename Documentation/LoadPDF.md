# PDF加载

​	支持多种多样的pdf格式加载。

- 首先支持使用工具栏进行pdf文件的导入，用户只需要在弹出的文件选择框中选择 pdf对应的文件，即可将之导入到软件打开并进行一定的浏览，
- 其次也支持从外部直接拖入pdf文件图标进入软件窗口后，pdf软件程序将会自动识别。对应的pdf文件并将之载入打开，这个文件可以是指向一个pdf文件的快捷方式，也可以是pdf文件本身。

> 文件的PDF后缀支持格式：.pdf与.PDF
